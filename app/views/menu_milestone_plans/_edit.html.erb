<h2>Milestone Plans</h2>
<%= error_messages_for 'version' %>

<div class="box">
	<% form_for :version, @version, :url => {:controller => 'menu_milestone_plans', :action => 'update', :project_id => @project, :version_id => @version} do |f| %>
		
		<%= render :partial => 'menu_milestone_plans/form', :locals => { :button_label => l(:button_update) , :project => @project, :release_id => params[:release_id], :f => f, :form_action => 'update'} %>

	<% end %>
</div>
